# Middlesbrough-Team-Input-via-JSON

This takes a JSON file that contains information on Boro players and allows the user to pull the info in via JS via AJAX. You can also filter the information as well
